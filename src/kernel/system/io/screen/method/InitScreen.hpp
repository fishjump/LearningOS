#pragma once

#include "../Screen.hpp"

void system::io::Screen::initScreen()
{
    if (instance == nullptr)
    {
        // instance = new Screen;
    }
    else
    {
        // no need to initialize
        // maybe log something later
    }
}